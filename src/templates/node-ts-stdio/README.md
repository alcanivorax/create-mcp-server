# **{{projectName}}**

A minimal MCP server implemented in TypeScript using stdio transport.

## Requirements

- Node 18+

## Setup

```bash
{{pm}} install
```

## Running the server

```bash
{{pm}} run dev
```

## Claude Desktop configuration

```bash
{
  "command": "{{pm}}",
   "args": ["run", "."]
}
```
